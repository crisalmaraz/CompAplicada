#programa para calcular el area de un triangulo
import math
print("Ingrese la base y altura separados por enter")
base,altura=float(input()), float(input());
area=(base*altura)/2; 

print(f"El triangulo de base {base} y altura {altura} tiene una area de {area}");